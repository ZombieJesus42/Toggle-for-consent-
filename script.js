
document.addEventListener("DOMContentLoaded", function () {
  // This would normally fetch signature count from a backend
  const fakeCount = 42;
  document.getElementById("sig-count").textContent = fakeCount;
});
